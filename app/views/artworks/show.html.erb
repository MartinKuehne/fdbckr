<div class="container">
   <div class="d-flex mt-5">
    <%= image_tag(@artwork.user.avatar, class: 'profile') %>
    <div class="container ml-3 top-description">
      <h1>My greatest artwork title</h1>
      <p>@<%= link_to @artwork.user.username, profile_path(@artwork.user) %></p>
    </div>
    <div class="button-wrapper">
      <a href="#" class="button-primary">Follow</a>
      <a href="#" class="button-shadow">Follow</a>
    </div>
  </div>

  <div class="d-flex justify-content-center">
    <% @artwork.photos.each do |image|%>
      <div class="image-wrapper">
        <%= cl_image_tag image.key, class: 'mt-3 mb-3', :alt => "artwork", style: 'object-fit: cover; max-width: 1200px; max-height: 1200px' %>
        <% @marked_comments.each do |comment| %>
          <i class="fas fa-comment marker", style="top: <%= comment.y_offset%>px; left: <%=comment.x_offset%>px"></i>
        <% end %>
      </div>
    <% end %>
  </div>
  
  <ul class="nav justify-content-center nav-tabs mt-3" id="myTab" role="tablist">
  <li class="nav-item" role="presentation">
    <button class="nav-link active" id="general_comments-tab" data-bs-toggle="tab" data-bs-target="#general_comments" type="button" role="tab" aria-controls="general_comments" aria-selected="true">General comments</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="marked_comments-tab" data-bs-toggle="tab" data-bs-target="#marked_comments" type="button" role="tab" aria-controls="marked_comments" aria-selected="false">Marked comments</button>
  </li>
</ul>

  <div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="general_comments" role="tabpanel" aria-labelledby="general_comments-tab">
      <div class="container d-flex mt-3">
        <img class="profile mr-3" src=<%= current_user.avatar%> alt="profile picture">
        <%= render  "comment_form" %>
      </div>
      <%= render  "general_comments" %>  
    </div>
    <div class="tab-pane fade" id="marked_comments" role="tabpanel" aria-labelledby="marked_comments-tab">
      <div class="container d-flex mt-3">
        <img class="profile mr-3" src=<%= current_user.avatar%> alt="profile picture">
        <%= render  "comment_form" %>
      </div>
      <%= render  "marked_comments" %> 
    </div>
  </div>
</div>